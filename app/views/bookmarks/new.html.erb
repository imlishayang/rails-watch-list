<h1>Create new bookmark</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :list %>
  <%= f.association :movie %>
  <%= f.button :submit, class: "btn btn-primary" %> <%= link_to "Cancel", lists_path %>
<% end %>
