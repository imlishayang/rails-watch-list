
<h1 class="title"> Movie lists </h1>

<% @lists.each do |list| %>
  <%= link_to list_path(list) do %>
   <div class="card">
    <li><%= list.name %></li>
    </div>
    <% end %>
<% end %>

<%= link_to "Create new list", new_list_path, class: "btn btn-primary" %>
