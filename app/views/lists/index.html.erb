<div id="navbar">
  <%# <a href="#home">Home</a>
  <div class="search-container">

  </div> %>
  <div class="submit-container">
 <%= simple_form_for(@list) do |f| %>
      <%= f.input :name, class:"input-size", placeholder:" List name",  label: false %>
      <%= f.submit 'Add to list', class:"submit-list" %>
    <% end %>
    </div>
</div>

<div class="new-section-list"> Your movie lists </div>

<div class="container">
  <div class="cards">
    <% @lists.each do |list| %>
        <div class="card-category">
            <%= link_to list_path(list) do %>
            <%= list.name %> <%= link_to "(X)", list_path(list), method: :delete, data: { confirm: "Are you sure you want to delete this list?"} %>
          <% end %>
          </div>
        <% end %>
    </div>
  </div>
